export const ROOT_ROUTE = () => "/"

export const TICKETS_ROUTE = () => "/tickets"
export const TICKET_BY_ID_ROUTE = (id: string) => `/tickets/${id}`
export const EDIT_TICKET_BY_ID_ROUTE = (id: string) => `/tickets/${id}/update`
